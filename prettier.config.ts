import { type Config } from "prettier";

const config: Config = {
    semi: true,
    singleQuote: true,
    trailingComma: "es5",
    tabWidth: 4,
    printWidth: 100,
    bracketSpacing: true,
};

export default config;
